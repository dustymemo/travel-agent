import { PlanChat } from "@/components/plan/PlanChat";

/**
 * Plan — the home route and heart of Roam (TA-17). A Server Component that
 * renders the client Plan experience: chat with Roam, watch the itinerary build.
 */
export default function PlanPage() {
  return <PlanChat />;
}
